{
  "name": "bearing-dev",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
